data:
  name: videocoatt
  dataset_dir : data/VideoCoAtt_Dataset
  saliency_dataset_dir : data/deepgaze_output_loader

exp_set:
  save_folder : saved_weights
  # model_name: videocoatt-head_pose_estimator
  # model_name: videocoatt-concat_independent
  # model_name: videocoatt-concat_independent_wo_img
  # model_name: videocoatt-concat_independent_wo_gaze
  # model_name: videocoatt-concat_independent_wo_position
  # model_name: videocoatt-concat_independent_wo_people_trans
  model_name: videocoatt-concat_independent_w_attraction
  # model_name: videocoatt-concat_independent_w_repulstion
  # model_name: videocoatt-concat_independent_w_attraction_w_repulstion
  # model_name: videocoatt-concat_independent_w_attraction_coord
  # model_name: videocoatt-concat_independent_w_repulstion_coord
  # model_name: videocoatt-concat_independent_w_attraction_coord_w_repulstion_coord
  # model_name : videocoatt-concat_independent_w_angle_rgb_feat
  # model_name : videocoatt-concat_independent_w_angle_rgb_feat_w_repulsion

  seed_num : 777
  gpu_mode : True
  gpu_start : 7
  gpu_finish : 7
  num_workers : 1
  batch_size : 1
  wandb_name : test

  mode: test
  # mode : validate
  # mode : train

exp_params:
  test_heads_type : det
  # test_heads_type : gt
  det_heads_model : dets_txt_nk_faster_rcnn_20220421
  test_heads_conf : 0.6
  # test_heads_conf : 0.8

  test_gt_gaze : False
  # test_gt_gaze : True

  use_frame_type: mid
  # use_frame_type: all